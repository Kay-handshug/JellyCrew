package handshug.jellycrew.home.view.dialog

import android.app.Activity
import androidx.appcompat.app.AlertDialog
import handshug.jellycrew.base.BaseDialog
import handshug.jellycrew.home.viewModel.HomeViewModel

class HomeDialog(private val activity: Activity, private val viewModel: HomeViewModel): BaseDialog() {
}